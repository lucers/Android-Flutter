class FormBarModel {
  bool editable;
  bool showHelp;
  bool showSearch;
  bool titleSelectable;
  String title;
  String subtitle;

  FormBarModel(this.editable, this.showHelp, this.showSearch, this.titleSelectable, this.title, this.subtitle);
}
